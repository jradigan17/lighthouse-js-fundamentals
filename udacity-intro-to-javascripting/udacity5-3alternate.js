let y = "";
function buildtriangle (length){
for (let x = 1; x <= length; x++) {
    y += "* ";
    console.log(y);
  }
}
buildtriangle(10);

let a = "";
for (let b = 1; b <= 10; b++) {
    a += "* ";
    console.log(a);
  }